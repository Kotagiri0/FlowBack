# 🔄 FlowBack - Система сбора и анализа клиентского фидбека

## 📋 Описание

FlowBack - это комплексная система для сбора, анализа и управления клиентским фидбеком с поддержкой NPS, CSAT и CES метрик.

## 🎯 Основные функции

1. **Сегментированный сбор фидбека** - разные опросы для ЛПР, техспецов, бизнес-пользователей
2. **Автоматические триггеры** - отправка опросов после демо, спринтов, релизов, инцидентов
3. **Мультиканальность** - Telegram/VK Teams боты + веб-формы + email
4. **Метрики NPS/CSAT/CES** - стандартизированные показатели удовлетворенности
5. **NLP-анализ** - автоматическая обработка текстовых ответов
6. **Админ-панель** - управление опросами, клиентами, шаблонами
7. **Дашборды аналитики** - визуализация метрик, динамика, сегментация
8. **Интеграции** - CRM (Bitrix24), Jira, мессенджеры через API
9. **Экспорт данных** - CSV, XLSX для внешней аналитики
10. **Управление частотой** - лимиты на опросы (не чаще 45 дней на роль)

## 📁 Структура проекта

```
flowback/
├── index.html              # Главная страница
├── css/                    # Стили
│   ├── variables.css       # CSS переменные
│   ├── base.css           # Базовые стили
│   ├── components.css     # Компоненты
│   ├── layout.css         # Макет
│   ├── dashboard.css      # Дашборд
│   ├── modals.css         # Модальные окна
│   └── responsive.css     # Адаптивность
├── js/                    # JavaScript модули
│   ├── config.js          # Конфигурация
│   ├── state.js           # Состояние приложения
│   ├── utils.js           # Утилиты
│   ├── api.js             # API запросы
│   ├── navigation.js      # Навигация
│   ├── dashboard.js       # Дашборд
│   ├── surveys.js         # Опросы
│   ├── clients.js         # Клиенты
│   ├── analytics.js       # Аналитика
│   ├── feedback.js        # Фидбек
│   ├── modals.js          # Модальные окна
│   └── main.js            # Инициализация
└── data/                  # Данные (JSON)
    ├── surveys.json       # Опросы
    ├── clients.json       # Клиенты
    ├── metrics.json       # Метрики
    └── feedback.json      # Фидбек
```

## 🚀 Быстрый старт

### Установка

1. Клонируйте репозиторий или скачайте файлы
2. Откройте `index.html` в браузере

Никаких дополнительных зависимостей не требуется! Все работает из коробки.

### Внешние библиотеки

Проект использует только одну внешнюю библиотеку:
- **Chart.js** (через CDN) - для графиков и визуализации данных

## 💻 Разработка

### Добавление нового модуля

1. Создайте новый JS файл в папке `js/`
2. Добавьте подключение в `index.html` перед `main.js`
3. Инициализируйте модуль в `main.js`

Пример:
```javascript
// js/mymodule.js
const MyModule = {
  init() {
    console.log('MyModule initialized');
  }
};

// В main.js
MyModule.init();
```

### Изменение стилей

Все стили разделены по модулям:
- Глобальные переменные → `css/variables.css`
- Компоненты (кнопки, формы) → `css/components.css`
- Макет страницы → `css/layout.css`
- Специфичные секции → соответствующие файлы

### Работа с данными

Данные хранятся в `State` объекте (`js/state.js`) и JSON файлах в папке `data/`.

Для реальной интеграции с backend замените методы в `js/api.js` на настоящие API запросы:

```javascript
// Пример замены заглушки
async getSurveys() {
  const response = await fetch(CONFIG.API.BASE_URL + '/surveys');
  return response.json();
}
```

## 🎨 Кастомизация

### Цвета

Измените цветовую схему в `css/variables.css`:

```css
:root {
  --primary-color: #667eea;  /* Ваш цвет */
  --secondary-color: #764ba2; /* Ваш цвет */
}
```

### Метрики

Добавьте новые метрики в `js/config.js`:

```javascript
METRICS: {
  NPS: 'nps',
  CSAT: 'csat',
  CES: 'ces',
  YOUR_METRIC: 'your_metric'
}
```

## 📱 Адаптивность

Дизайн полностью адаптивен для:
- Desktop (1400px+)
- Tablet (768px - 1400px)
- Mobile (< 768px)

Брейкпоинты настраиваются в `css/responsive.css`.

## 🔧 API интеграция

### Текущая реализация

Сейчас все данные берутся из `State` (локальное хранилище в JS).

### Подключение реального API

1. Настройте эндпоинты в `js/config.js`:
```javascript
API: {
  BASE_URL: 'https://your-api.com/v1',
  SURVEYS: '/surveys',
  CLIENTS: '/clients',
  ...
}
```

2. Замените методы в `js/api.js` на реальные fetch запросы

3. Добавьте обработку ошибок и загрузки

## 📊 Метрики

### NPS (Net Promoter Score)
- Шкала: 0-10
- Формула: (% Промоутеров - % Детракторов)
- Промоутеры: 9-10
- Пассивные: 7-8
- Детракторы: 0-6

### CSAT (Customer Satisfaction)
- Шкала: 1-5
- Среднее арифметическое всех оценок

### CES (Customer Effort Score)
- Шкала: 1-5
- Чем ниже, тем лучше (меньше усилий)

## 🐛 Отладка

Откройте консоль браузера (F12) для просмотра логов:
- `✅` - успешная инициализация
- `❌` - ошибки
- Логи навигации и действий пользователя

## 📝 TODO

- [ ] Реальная интеграция с API
- [ ] Авторизация и роли пользователей
- [ ] Экспорт в XLSX (сейчас только CSV)
- [ ] Push-уведомления
- [ ] Темная тема
- [ ] Мультиязычность
- [ ] Расширенная NLP аналитика
- [ ] Интеграция с Telegram/VK Teams ботами
- [ ] Webhook'и для триггеров
- [ ] A/B тестирование опросов

## 📄 Лицензия

MIT License - используйте как хотите!

## 👥 Контакты

- Email: support@flowback.ru
- Telegram: @flowback_support

## 🙏 Благодарности

- Chart.js за отличную библиотеку графиков
- Сообществу за фидбек и предложения

---

**Версия:** 1.0.0  
**Дата:** Декабрь 2024